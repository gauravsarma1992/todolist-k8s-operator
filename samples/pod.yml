apiVersion: v1
kind: Pod
metadata:
  name: jack
  namespace: operator-namespace
spec:
  containers:
  - name: ubuntu
    image: ubuntu:latest
    # Just sleep forever
    command: [ "sleep" ]
    args: [ "infinity" ]
